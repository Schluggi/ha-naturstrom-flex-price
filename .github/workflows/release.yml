name: Release

on:
  push:
    branches: [ main ]

jobs:
  release:
    name: 🚀 Release
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write

    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: 🔖 Semantic Release
      uses: cycjimmy/semantic-release-action@v3
      with:
        semantic_version: 1.1.0
        extra_plugins: |
          @semantic-release/changelog
          @semantic-release/git
        update_file: custom_components/naturstrom_flex/manifest.json
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}